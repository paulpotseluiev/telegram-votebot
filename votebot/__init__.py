"""votebot — вікторина-чарт 3×3 у Telegram-каналі.

Пакет свідомо розділений так, щоб кожен модуль тестувався окремо:
    config  — параметри з config.yaml + секрети з .env
    render  — малювання чарта і анімації (жодних мережевих викликів)
    state   — атомарне сховище стану активної вікторини
    api     — тонка обгортка Bot API
    updates — злив getUpdates, накопичення заявок і реакцій
    rounds  — життєвий цикл раунду (старт / відлік / підбиття підсумків)
"""

__version__ = "0.1.0"
